import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { ClientProgress, ClientSubmission, getPublicPaymentDisclosure, getPublicScheduleDetails } from "@/lib/clientPortal";
import { PaymentState, formatCurrency, formatDisplayDate, statusMeta } from "@/lib/commission";
import { ChevronDown, Eye, KeyRound, LogIn, ShieldCheck } from "lucide-react";
import { useState } from "react";

export function ClientProgressPanel({ signedInWithGoogle, accountEmail, code, setCode, codeProgress, myProgress, mySubmissions, searching, onLookup, onGoogle, onSignOut }: { signedInWithGoogle: boolean; accountEmail: string; code: string; setCode: (value: string) => void; codeProgress: ClientProgress | null; myProgress: ClientProgress[]; mySubmissions: ClientSubmission[]; searching: boolean; onLookup: () => Promise<void>; onGoogle: () => Promise<void>; onSignOut: () => void }) {
  const progress = codeProgress ? [codeProgress] : myProgress;
  return <section className="mt-5 rounded-[2rem] border border-[#cfd9cf] bg-[#fffdfa] p-5 shadow-[0_14px_32px_rgba(40,59,49,.06)] sm:p-8"><div className="flex flex-wrap items-start justify-between gap-4"><div><h2 className="font-display text-2xl">畫約遞臻</h2><p className="mt-1 text-sm leading-6 text-[#456153]">此頁顯示繪師公開給寄墨主的畫約、款項與作畫進度。</p></div>{signedInWithGoogle ? <div className="rounded-xl bg-[#edf5ed] px-3 py-2 text-xs text-[#355b48]"><ShieldCheck className="mr-1 inline h-3.5 w-3.5" /><span>{accountEmail}</span><button className="ml-2 underline" onClick={onSignOut}>登出</button></div> : <Button variant="outline" className="border-[#b9cdbd] text-[#355b48]" onClick={() => void onGoogle()}><LogIn className="mr-1.5 h-4 w-4" />使用 Google 帳號</Button>}</div><div className="mt-6 rounded-2xl bg-[#edf5ed] p-4"><Label className="text-[#355b48]">沒有 Google 帳號？輸入對契符節</Label><div className="mt-2 flex flex-col gap-2 sm:flex-row"><Input value={code} onChange={(event) => setCode(event.target.value)} placeholder="HY-xxxxxxxxxxxxxxxx-xxxxxxxxxxxxxxxx-xxxxxxxxxxxxxxxx" /><Button variant="outline" className="border-[#b9cdbd] text-[#355b48]" disabled={searching} onClick={() => void onLookup()}><KeyRound className="mr-1.5 h-4 w-4" />{searching ? "查詢中…" : "檢視遞臻"}</Button></div></div><div className="mt-6 space-y-3">{progress.map((item) => <ProgressCard key={item.id} progress={item} />)}{mySubmissions.length > 0 && <div className="rounded-2xl border border-dashed border-[#cfd9cf] p-4"><p className="font-medium">已寄出的墨諾函箋</p><div className="mt-2 space-y-1 text-sm text-[#456153]">{mySubmissions.map((item) => <p key={item.id}>· {item.state === "submitted" ? "等待繪師啟函" : item.state === "accepted" ? "已轉為畫約" : "未受理"} · {new Date(item.createdAt).toLocaleDateString("zh-TW")}</p>)}</div></div>}{progress.length === 0 && <div className="rounded-2xl border border-dashed border-[#cfd9cf] p-8 text-center text-sm leading-6 text-[#6c7e70]">尚未找到已公開的畫約。若您剛寄出墨諾函箋，請等候繪師確認；也可使用繪師提供的對契符節檢視遞臻。</div>}</div></section>;
}

function ProgressCard({ progress }: { progress: ClientProgress }) {
  const [open, setOpen] = useState(false);
  const badge = progress.isRush ? "飛墨急案" : progress.scheduleType === "reservation" ? "先寄墨諾" : null;
  const scheduleDetails = getPublicScheduleDetails(progress);
  const paymentDisclosure = progress.paymentDisclosure ?? getPublicPaymentDisclosure(progress.status);
  const paymentText = (state: PaymentState | undefined, paidAt: number | null | undefined) => state === "paid" ? `已繳 · ${formatDisplayDate(paidAt)}` : state === "unpaid" ? "待繳" : "待繪師補登";
  return <article className="rounded-2xl border border-[#cfd9cf] p-4"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="font-display text-lg font-semibold">{progress.clientName} 的畫約</p><p className="mt-1 text-xs text-[#6c7e70]">{progress.orderCode}</p></div><div className="flex flex-wrap justify-end gap-1.5">{badge && <span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${progress.isRush ? "bg-[#fff0e9] text-[#a9573c]" : "bg-[#f3eef9] text-[#6b4d83]"}`}>{badge}</span>}<span className="rounded-full bg-[#edf5ed] px-2.5 py-1 text-xs font-semibold text-[#355b48]">{progress.statusLabel}</span></div></div><p className="mt-4 text-sm leading-6 text-[#456153]">{progress.nextStep}</p>{scheduleDetails.length > 0 && <div className="mt-4 grid gap-2 text-sm sm:grid-cols-2">{scheduleDetails.map((detail) => <p key={detail.label}><span className="text-[#6c7e70]">{detail.label}：</span>{detail.value}</p>)}</div>}<Button type="button" variant="outline" size="sm" className="mt-4 border-[#b9cdbd] text-[#355b48]" onClick={() => setOpen((value) => !value)}><Eye className="mr-1.5 h-4 w-4" />{open ? "收起內容" : "檢視內容"}<ChevronDown className={`ml-1.5 h-3.5 w-3.5 transition ${open ? "rotate-180" : ""}`} /></Button>{open && <div className="mt-4 space-y-4 border-t border-[#d8ded5] pt-4"><div><p className="text-xs font-semibold tracking-[.1em] text-[#6c7e70]">作畫項目</p><div className="mt-2 space-y-2">{progress.artworkItems?.length ? progress.artworkItems.map((item) => <p key={item.id} className="rounded-xl bg-[#f6f7f2] px-3 py-2 text-sm text-[#355b48]">{item.summary}</p>) : <p className="text-sm text-[#6c7e70]">繪師確認後提供。</p>}</div></div><div className="grid gap-3 rounded-xl bg-[#f6f7f2] p-3 text-sm sm:grid-cols-2"><p><span className="text-[#6c7e70]">委託日：</span>{formatDisplayDate(progress.createdAt)}</p>{paymentDisclosure === "estimate" && <p><span className="text-[#6c7e70]">預估底價：</span>NT$ {formatCurrency(progress.estimatedBaseAmount)}<span className="block pt-1 text-xs text-[#6c7e70]">僅供參考，將依確認需求調整。</span></p>}{paymentDisclosure === "deposit" && <p><span className="text-[#6c7e70]">訂金：</span>NT$ {formatCurrency(progress.depositAmount)} · {paymentText(progress.depositState, progress.depositPaidAt)}</p>}{paymentDisclosure === "total" && <><p><span className="text-[#6c7e70]">總額：</span>NT$ {formatCurrency(progress.totalAmount)}</p><p><span className="text-[#6c7e70]">訂金：</span>NT$ {formatCurrency(progress.depositAmount)} · {paymentText(progress.depositState, progress.depositPaidAt)}</p><p><span className="text-[#6c7e70]">尾款：</span>NT$ {formatCurrency(progress.balanceAmount)} · {paymentText(progress.balanceState, progress.balancePaidAt)}</p></>}{paymentDisclosure === "hidden" && <p className="text-[#6c7e70]">款項資訊已依畫約階段收起；如需確認，請聯繫繪師。</p>}</div><div><p className="text-xs font-semibold tracking-[.1em] text-[#6c7e70]">進度</p><div className="mt-2 space-y-1.5 text-sm">{progress.statusHistory?.length ? progress.statusHistory.slice().reverse().map((entry, index) => <p key={`${entry.at}-${index}`}><span className="font-medium text-[#355b48]">{statusMeta[entry.status].label}</span><span className="text-[#6c7e70]"> · {formatDisplayDate(entry.at)}</span></p>) : <p className="text-[#6c7e70]">目前為 {progress.statusLabel}</p>}</div></div></div>}</article>;
}
